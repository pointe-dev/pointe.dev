# Rust
target/
# Cargo.lock is committed for reproducible binary builds
**/*.rs.bk
*.pdb

# Node
node_modules/
dist/
.next/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.prod
.env.n8n
.env.langfuse
.env.staging
!.env.staging.example
secrets/

# Claude memory backup (local only)
claude-memory/

# FastEmbed model cache (auto-downloaded at runtime, never commit)
.fastembed_cache/

# n8n MCP config — contient un token secret, ne jamais committer
n8n.mcp
