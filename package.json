{
  "name": "pointe-dev",
  "version": "0.1.0",
  "description": "AI Product Commercialization & Business Process Automation",
  "scripts": {
    "tailwind:build": "tailwindcss -i ./crates/frontend/input.css -o ./crates/frontend/styles.css",
    "wasm:build": "wasm-pack build crates/frontend --target web --out-dir pkg --release",
    "dev": "npm run tailwind:build && npm run wasm:build && cargo build --release && npm run serve",
    "build": "npm run tailwind:build && npm run wasm:build && cargo build --release",
    "serve": "cd crates/backend && cargo run --release",
    "lhci": "lhci autorun",
    "e2e": "cd tests/e2e && npx playwright test"
  },
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.49.0",
    "tailwindcss": "^3.4.0"
  },
  "dependencies": {
    "wasm-pack": "^0.15.0"
  }
}
